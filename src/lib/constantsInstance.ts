import { ControlConstants } from "@lds/bridge-modal";

import { PRODUCT_MODULE_NAME } from "../constants";
export const baseInfo = {
  productName: "模板项目",
  productKey: PRODUCT_MODULE_NAME,
};
export const constants = new ControlConstants(baseInfo);
