module.exports = {
  /** 项目名，作为支付中心和打点的产品名 */
  projectName: "proc_opt",
  /** 中文标题 */
  title: "模板项目",
  /** 是否使用路由 */
  useRoutes: true,
  /** 是否使用mpa（适用于需要同时展示多个窗口的情况） */
  multiEntry: true,
  /** 是否启用 Lerna 模板（Monorepo） */
  lerna: false,
  /** 是否启用 biome */
  biome: false,
};
